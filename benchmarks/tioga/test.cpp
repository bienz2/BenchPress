#include "mpi.h"
#include "hip/hip_runtime_api.h"

int main(int argc, char* argv[])
{
    MPI_Init(&argc, &argv);

    float* data = new float[2048];

    for (int

    MPI_Finalize();
    return 0;
}
